import { MinitranslatePipe } from './minitranslate.pipe';

describe('MinitranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new MinitranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
